name: CI do Gerenciador de Gastos

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Fazer Checkout do Código
      uses: actions/checkout@v4

    - name: Configurar o .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.x'

    - name: Restaurar Dependências
      run: dotnet restore **/*.csproj 

    - name: Executar Linting (Análise Estática)
      # Verifica se o código segue os padrões de formatação do C#
      run: dotnet format **/*.csproj --verify-no-changes

    - name: Compilar Projeto
      run: dotnet build **/*.csproj --no-restore

    - name: Executar Testes Automatizados
      run: dotnet test **/*.csproj --no-build --verbosity normal
